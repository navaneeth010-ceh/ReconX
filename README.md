# ReconX
ReconX is an automated web reconnaissance tool designed to streamline the process of information gathering and attack surface mapping in web security assessments. The tool takes a target domain as input and performs passive and active reconnaissance to identify subdomains, detect live hosts, and gather key metadata about exposed services.
